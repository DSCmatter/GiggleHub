import { defineConfig } from 'vite'

export default defineConfig({
  // Configuration options
  server: {
    port: 3000,  // Optional: specify the port for the dev server
  },
  // You can add more configuration options here
})
